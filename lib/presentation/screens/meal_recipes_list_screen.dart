import 'package:flutter/material.dart';

class MealRecipesListScreen extends StatelessWidget {
  const MealRecipesListScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
